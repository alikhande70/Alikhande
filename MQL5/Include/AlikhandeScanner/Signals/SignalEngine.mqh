#pragma once
#include "../Domain/Models.mqh"
#include "../Core/VersionInfo.mqh"
#include "../Core/Hash.mqh"
#include "../Core/Config.mqh"
class AS_SignalEngine {
private:
   double Clamp(double v){return MathMax(0.0,MathMin(100.0,v));}
   void NearestZones(const AS_Zone &zones[],const AS_SymbolSnapshot &snap,int &support_index,int &resistance_index){support_index=-1;resistance_index=-1;for(int i=0;i<ArraySize(zones);i++){if(!zones[i].valid||zones[i].broken)continue;if(zones[i].high<=snap.bid&&(support_index<0||zones[i].high>zones[support_index].high))support_index=i;if(zones[i].low>=snap.ask&&(resistance_index<0||zones[i].low<zones[resistance_index].low))resistance_index=i;}}
public:
   bool Evaluate(const string symbol,const AS_TrendResult &h4,const AS_TrendResult &h1,const AS_TrendResult &m15,const AS_TrendResult &m5,const AS_SymbolSnapshot &snap,const AS_Zone &zones[],const AS_RegimeResult &regime,const AS_SymbolSpec &spec,AS_SignalCandidate &s){ZeroMemory(s);if(!h4.valid||!h1.valid||!m15.valid||!m5.valid||!spec.ready)return false;int si,ri;NearestZones(zones,snap,si,ri);double support=(si>=0?zones[si].high:0),resistance=(ri>=0?zones[ri].low:0);bool near_support=(si>=0&&snap.bid>=zones[si].low-h1.atr*0.10&&snap.bid-zones[si].high<=h1.atr*1.50);bool near_resistance=(ri>=0&&snap.ask<=zones[ri].high+h1.atr*0.10&&zones[ri].low-snap.ask<=h1.atr*1.50);ENUM_AS_SETUP_TYPE long_setup=AS_SETUP_NONE,short_setup=AS_SETUP_NONE;if(h1.direction_score>=20&&near_support&&m15.direction_score>-20&&m5.direction_score>=15)long_setup=AS_TREND_PULLBACK;else if(h1.direction_score>-45&&near_support&&m15.direction_score>=0&&m5.direction_score>=20)long_setup=AS_SUPPORT_REJECTION;if(h1.direction_score<=-20&&near_resistance&&m15.direction_score<20&&m5.direction_score<=-15)short_setup=AS_TREND_PULLBACK;else if(h1.direction_score<45&&near_resistance&&m15.direction_score<=0&&m5.direction_score<=-20)short_setup=AS_RESISTANCE_REJECTION;double lb=0,sb=0;string lr="",sr="";if(h4.direction_score>0){lb+=10;lr+="H4_LONG;";}else if(h4.direction_score<0){sb+=10;sr+="H4_SHORT;";}if(h1.direction_score>=20){lb+=20+MathMin(10.0,h1.strength*0.10);lr+="H1_BULL;";}else if(h1.direction_score<=-20){sb+=20+MathMin(10.0,h1.strength*0.10);sr+="H1_BEAR;";}if(long_setup!=AS_SETUP_NONE){lb+=25;lr+="LONG_SETUP;";}if(short_setup!=AS_SETUP_NONE){sb+=25;sr+="SHORT_SETUP;";}if(si>=0)lb+=MathMin(15.0,zones[si].quality*0.15);if(ri>=0)sb+=MathMin(15.0,zones[ri].quality*0.15);if(m5.direction_score>=15)lb+=15;else if(m5.direction_score<=-15)sb+=15;if(h1.direction_score>0&&m15.direction_score>0&&m5.direction_score>0)lb+=10;if(h1.direction_score<0&&m15.direction_score<0&&m5.direction_score<0)sb+=10;if(snap.spread_state==AS_SPREAD_NORMAL){lb+=5;sb+=5;}double lp=0,sp=0;bool hard=false;string codes="";if(!regime.tradable){lp+=15;sp+=15;codes+="REGIME_NOT_READY;";}if(snap.spread_state==AS_SPREAD_HIGH||snap.spread_state==AS_SPREAD_EXTREME||snap.spread_state==AS_SPREAD_STALE||snap.spread_state==AS_SPREAD_NO_TICK||snap.spread_state==AS_SPREAD_WARMING_UP){hard=true;codes+="SPREAD_OR_QUOTE_BLOCK;";}s.long_score=Clamp(lb-lp);s.short_score=Clamp(sb-sp);s.direction=AS_DIR_NONE;s.setup=AS_SETUP_NONE;if(!hard&&regime.tradable&&long_setup!=AS_SETUP_NONE&&s.long_score>=75&&s.long_score-s.short_score>=10){s.direction=AS_DIR_LONG;s.setup=long_setup;}else if(!hard&&regime.tradable&&short_setup!=AS_SETUP_NONE&&s.short_score>=75&&s.short_score-s.long_score>=10){s.direction=AS_DIR_SHORT;s.setup=short_setup;}double entry=(s.direction==AS_DIR_SHORT?snap.bid:snap.ask),buffer=MathMax(h1.atr*0.15,snap.point*5),stop=0;if(s.direction==AS_DIR_LONG){if(si<0){hard=true;codes+="NO_STRUCTURAL_SUPPORT;";}else stop=zones[si].low-buffer;}if(s.direction==AS_DIR_SHORT){if(ri<0){hard=true;codes+="NO_STRUCTURAL_RESISTANCE;";}else stop=zones[ri].high+buffer;}double rd=(stop>0?MathAbs(entry-stop):0),tp=0;if(s.direction!=AS_DIR_NONE){if(rd<=0||rd>h1.atr*2.50){hard=true;codes+="INVALID_STRUCTURAL_STOP;";}else tp=(s.direction==AS_DIR_LONG?entry+AS_MINIMUM_RISK_REWARD*rd:entry-AS_MINIMUM_RISK_REWARD*rd);if(s.direction==AS_DIR_LONG&&ri>=0&&zones[ri].low<tp){hard=true;codes+="OPPOSING_ZONE_BEFORE_2R;";}if(s.direction==AS_DIR_SHORT&&si>=0&&zones[si].high>tp){hard=true;codes+="OPPOSING_ZONE_BEFORE_2R;";}}if(hard)s.direction=AS_DIR_NONE;s.symbol=symbol;s.preferred_entry=entry;s.stop_loss=stop;s.take_profit=tp;s.nearest_support=support;s.nearest_resistance=resistance;s.creation_time=TimeCurrent();s.confirmation_bar_time=m5.available_information_time;s.expires_at=s.creation_time+AS_DEFAULT_SIGNAL_TTL_SECONDS;s.rule_version=AS_RULE_VERSION;s.scoring_version=AS_SCORING_VERSION;s.parameter_hash=AS_Fnv1a("alikhande-v1.2-baseline");s.broker_spec_hash=spec.spec_hash;s.reasons=(s.direction==AS_DIR_LONG?lr:(s.direction==AS_DIR_SHORT?sr:""));s.hard_blocked=hard;s.validation_codes=codes;s.regime=regime.regime;s.regime_confidence=regime.confidence;s.state=(s.direction==AS_DIR_NONE?AS_SIGNAL_WATCH:AS_SIGNAL_CONFIRMED);s.has_historical_estimate=false;s.historical_win_rate=0;s.sample_size=0;s.confidence_low=0;s.confidence_high=0;string key=StringFormat("%s|%d|%d|%I64d|%s|%s",symbol,(int)s.direction,(int)s.setup,(long)s.confirmation_bar_time,AS_RULE_VERSION,spec.spec_hash);s.signal_id=AS_Fnv1a(key);return true;}
};
